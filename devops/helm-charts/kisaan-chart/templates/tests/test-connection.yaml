apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kisaan-dot-com.fullname" . }}-test-connection"
  labels:
    {{- include "kisaan-dot-com.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kisaan-dot-com.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
